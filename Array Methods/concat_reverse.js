/* Concat() method
- The concat method in JavaScript is used to merge two or more arrays into a single new array.
- It doesn’t change the original arrays; instead, it returns a new array.

- Syntax:
    array1.concat(array2, array3, ...);

*/

// Concatenating two arrays

/* const array1 = [1,2,3];
const array2 = [11,22,33];

const combinedArray = array1.concat(array2);

console.log(combinedArray); // [ 1, 2, 3, 11, 22, 33 ]
 */





/* reverse() method
- The reverse method reverses the order of elements in an array.
- It modifies the original array.

- Syntax
    array.reverse();

*/

// Reversing an array of numbers

/* const numbers =[1,2,3,4];
numbers.reverse();

console.log(numbers); //[ 4, 3, 2, 1 ] */

// Reversing an array of strings

const letters = ["A", "B", "C"]
letters.reverse();
console.log(letters);  // [ 'C', 'B', 'A' ]
