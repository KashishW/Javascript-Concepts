/* Methods

- A method is a function that is a property of an object.

- Methods are defined within objects or classes.

*/

/* const car = {
    brand: 'Ford',
    start(){
        console.log(`${this.brand} is starting`);
    }
}
car.start();   */ // Ford is starting


// String Method:    toUpperCase(), toLowerCase()

/* let text = "hello";
console.log(text.toUpperCase());   */  // HELLO

// Array methods:  push(), pop

/* let numbers = [1,2,3,4];
numbers.push(5);
console.log(numbers); */  //[1,2,3,4,5]

// Chaining methods

let text = "  Javascript"

console.log(text.trim().toUpperCase());   // JAVASCRIPT









