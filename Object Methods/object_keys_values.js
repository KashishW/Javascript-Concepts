/* Object.keys(obj):

Returns an array of the object's property names (keys). 

*/

/* const person = {
    name:'Ram',
    age:10,
    country:'India'
}

const keys = Object.keys(person);

console.log(keys);  // [ 'name', 'age', 'country' ] */




/* Object.values(obj): 

Returns an array of the object's property values.

*/

const person = {
    name:'Ram',
    age:10,
    country:'India'
}

const values = Object.values(person);

console.log(values);  // [ 'Ram', 10, 'India' ]